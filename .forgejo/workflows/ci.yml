name: CI

on:
  push:
    branches:
      - dev

jobs:
  test:
    runs-on: codeberg-tiny

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
         node-version: 22

      - name: Install deps
        run: npm ci
